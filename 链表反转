链表反转                     图的优先深度搜索（DFS）
s->next=p->next；           visited[v]=true;
p->next=prew;               for(int u;adj[v]){
prew=cur;                   if(!visit=[u]) DFS(u,visited,adj);}
cur=next;                                
                                             二叉树遍历前序（根左右）            
合并有序链表                                  void preorder(TreeNode*root){
if（!l1）return l2;                         if(!root)return;
if(!l2) return l1;                          visit(root);
if(l1->val<=l2->val){                       preorder(root->left);
l1->next= mergeTwolists(l1-next,l2);        preorder(root->right);}  
return l1;}
else{
l2->next=mergeTwolists(l1,l2->next);           中序（左根右）
return l2;}                                    void inorder(TreeNode*root){
                                                if(!root)return;
快速排序（partition）                            inorder(root->left);
int pivot=arr[high];                             visit(root);
int i=low-1;                                    inorder(root->right);} 
for(int j=low;j<high;j++){
if(arr[j]<=pivot){
i++;                                           
swap(arr[i],arr[j]);}}                         后序（左右根）
swap(arr[i+1],arr[high]);                       void postorder(TreeNode*root){  
return i+1;                                     if(!root)return;
                                               postorder(root->left);
                                               postorder(root->right);  
堆的向下调整（heapify）                           visit(root);}
int largest=i;
int left=2*i+1;
int right=2*i+2;
if(left<n && arr[left]<arr[largest]) left=largest;
if(right<n && arr[right]<arr[largest]) right=largest;
if(largest!=i){
swap(arr[i],arr[largest]);
heapify(arr,n,largest)}


